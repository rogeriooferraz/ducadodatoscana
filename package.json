{
  "name": "site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "build:images": "node scripts/build.js --optimize-images",
    "test": "playwright test"
  },
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0"
  },
  "optionalDependencies": {
    "sharp": "^0.34.4"
  }
}
